package zadania_java_level0;

import java.util.Scanner;

public class Zadanie6 {
    public static void main(String[] args) {
        System.out.println("podaj rodzaj operacji + / * -");

        Scanner rd = new Scanner(System.in);
        String operation = rd.nextLine();

        System.out.println("podaj pierwszą i drugą liczbę do wykonania operacji: ");
        int firstDigit = rd.nextInt();
        int secondDigit = rd.nextInt();

        System.out.printf("wybrałeś operację %s oraz liczby %d %d",operation, firstDigit, secondDigit);

        switch(operation) {
            case("+"){ a+b;
            case("/"){ if(druga jest zerowa) {to wyjdz z swicza)
        }

    }
}
